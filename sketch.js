
function setup(){
  createCanvas(windowWidth,windowHeight);
  libSetup();
  messageOne = "welcome :)";
  messageTwo = "~(°_°~)";
}

function draw(){
  background(color1);
  // write your functions / sketch here
  typeDrawStart(width/2,height/2,100);
}
